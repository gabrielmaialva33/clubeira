<BackofficeComponents.shell
  account_scope={@current_account_scope}
  access={@backoffice_access}
  current_polo={@current_polo}
  polo_form={@polo_form}
  flash={@flash}
  active_section={:overview}
>
  <div class="mx-auto max-w-[1500px]">
    <section class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
      <div>
        <div class="clubeira-page-kicker">
          {gettext("Operational overview")}
        </div>
        <h1 class="clubeira-page-title mt-1.5">
          {gettext("Everything that moves %{polo}", polo: @current_polo.name)}
        </h1>
        <p class="clubeira-page-copy mt-1.5">
          {gettext("A current, capability-aware view of your club operation.")}
        </p>
      </div>
      <div class="inline-flex w-fit items-center gap-2 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700">
        <span class="size-2 rounded-full bg-emerald-500" />
        {gettext("Operation online")}
      </div>
    </section>

    <section class="mt-6 overflow-hidden rounded-xl border border-clubeira-line bg-white">
      <div class="grid lg:grid-cols-[minmax(320px,0.95fr)_minmax(0,1.4fr)]">
        <div class="clubeira-grid-paper flex gap-4 border-b border-clubeira-line bg-clubeira-blue-soft/60 p-5 sm:p-6 lg:border-b-0 lg:border-r">
          <span class="clubeira-icon-tile shrink-0">
            <.icon name="hero-chart-bar-square" class="size-5" />
          </span>
          <div>
            <p class="text-[10px] font-bold uppercase tracking-[0.16em] text-clubeira-blue">
              {gettext("Today in Clubeira")}
            </p>
            <h2 class="mt-2 max-w-md text-lg font-bold tracking-[-0.025em] text-clubeira-ink">
              {gettext("Decisions with context, without losing tenant isolation.")}
            </h2>
            <p class="mt-2 max-w-lg text-xs leading-5 text-clubeira-muted">
              {gettext(
                "Every module below reflects the roles currently authorized for this account and polo."
              )}
            </p>
          </div>
        </div>
        <div class="grid grid-cols-2 divide-x divide-y divide-clubeira-line lg:grid-cols-[repeat(auto-fit,minmax(112px,1fr))] lg:divide-y-0">
          <div
            id="metric-authorized-areas"
            class="p-5 sm:p-6"
          >
            <p class="text-2xl font-bold text-clubeira-ink">
              {length(@current_polo.capabilities)}
            </p>
            <p class="mt-1 text-[11px] font-semibold text-clubeira-muted">
              {gettext("Authorized areas")}
            </p>
          </div>
          <div
            :if={capability?(@current_polo, :manage_partners)}
            id="metric-recent-places"
            class="p-5 sm:p-6"
          >
            <p class="text-2xl font-bold text-clubeira-ink">{@preview_counts.places}</p>
            <p class="mt-1 text-[11px] font-semibold text-clubeira-muted">
              {gettext("Recent places")}
            </p>
          </div>
          <div
            :if={capability?(@current_polo, :manage_billing)}
            id="metric-recent-subscriptions"
            class="p-5 sm:p-6"
          >
            <p class="text-2xl font-bold text-clubeira-ink">{@preview_counts.subscriptions}</p>
            <p class="mt-1 text-[11px] font-semibold text-clubeira-muted">
              {gettext("Recent contracts")}
            </p>
          </div>
          <div
            :if={capability?(@current_polo, :manage_billing)}
            id="metric-recent-payments"
            class="bg-clubeira-orange-soft/55 p-5 sm:p-6"
          >
            <p class="text-2xl font-bold text-clubeira-orange">{@preview_counts.payments}</p>
            <p class="mt-1 text-[11px] font-semibold text-orange-800">
              {gettext("Recent payments")}
            </p>
          </div>
        </div>
      </div>
    </section>

    <div
      :if={@load_errors != []}
      id="dashboard-load-errors"
      class="mt-6 flex items-start gap-3 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900"
    >
      <.icon name="hero-exclamation-triangle" class="mt-0.5 size-5 shrink-0" />
      <p>{gettext("Some operational sections could not be loaded. Try refreshing the page.")}</p>
    </div>

    <section class="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">
      <div
        :if={capability?(@current_polo, :manage_partners)}
        id="capability-manage-partners"
        class="group rounded-xl border border-clubeira-line bg-white p-4 transition hover:border-blue-200"
      >
        <span class="clubeira-icon-tile">
          <.icon name="hero-building-storefront" class="size-5" />
        </span>
        <h3 class="mt-4 text-sm font-bold text-clubeira-ink">{gettext("Partner network")}</h3>
        <p class="mt-1.5 text-xs leading-5 text-slate-500">
          {gettext("Places, profiles and commercial catalog.")}
        </p>
      </div>

      <div
        :if={capability?(@current_polo, :manage_billing)}
        id="capability-manage-billing"
        class="group rounded-xl border border-clubeira-line bg-white p-4 transition hover:border-orange-200"
      >
        <span class="grid size-10 place-items-center rounded-lg bg-clubeira-orange-soft text-clubeira-orange">
          <.icon name="hero-banknotes" class="size-5" />
        </span>
        <h3 class="mt-4 text-sm font-bold text-clubeira-ink">{gettext("Billing")}</h3>
        <p class="mt-1.5 text-xs leading-5 text-slate-500">
          {gettext("Payments, subscriptions and financial health.")}
        </p>
      </div>

      <div
        :if={capability?(@current_polo, :moderate_reviews)}
        id="capability-moderate-reviews"
        class="group rounded-xl border border-clubeira-line bg-white p-4 transition hover:border-cyan-200"
      >
        <span class="grid size-10 place-items-center rounded-lg bg-cyan-50 text-cyan-600">
          <.icon name="hero-chat-bubble-left-right" class="size-5" />
        </span>
        <h3 class="mt-4 text-sm font-bold text-clubeira-ink">{gettext("Reputation")}</h3>
        <p class="mt-1.5 text-xs leading-5 text-slate-500">
          {gettext("Verified reviews and moderation queue.")}
        </p>
      </div>

      <div
        :if={capability?(@current_polo, :manage_operations)}
        id="capability-manage-operations"
        class="group rounded-xl border border-clubeira-line bg-white p-4 transition hover:border-violet-200"
      >
        <span class="grid size-10 place-items-center rounded-lg bg-violet-50 text-violet-600">
          <.icon name="hero-command-line" class="size-5" />
        </span>
        <h3 class="mt-4 text-sm font-bold text-clubeira-ink">{gettext("Operations")}</h3>
        <p class="mt-1.5 text-xs leading-5 text-slate-500">
          {gettext("Outbox, retries and audit trail.")}
        </p>
      </div>
    </section>

    <div class="mt-7 grid gap-6 xl:grid-cols-[1.05fr_0.95fr]">
      <section
        :if={capability?(@current_polo, :manage_partners)}
        id="places-section"
        class="overflow-hidden rounded-xl border border-clubeira-line bg-white"
      >
        <div class="flex items-center justify-between border-b border-slate-100 px-5 py-4 sm:px-6">
          <div>
            <h2 class="text-sm font-bold text-slate-900">{gettext("Partner places")}</h2>
            <p class="mt-1 text-xs text-slate-500">{gettext("Latest polo participations")}</p>
          </div>
          <span class="rounded-lg bg-blue-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-blue-700">
            {gettext("Live data")}
          </span>
        </div>
        <div id="places-feed" phx-update="stream" class="divide-y divide-slate-100">
          <div
            id="places-empty"
            class="hidden only:flex items-center gap-3 px-6 py-8 text-sm text-slate-500"
          >
            <.icon name="hero-building-storefront" class="size-5 text-slate-300" />
            {gettext("No places available in this polo.")}
          </div>
          <div
            :for={{id, place} <- @streams.places}
            id={id}
            class="flex items-center gap-4 px-5 py-4 transition hover:bg-slate-50/80 sm:px-6"
          >
            <span class="grid size-10 shrink-0 place-items-center rounded-xl bg-slate-100 text-xs font-bold text-slate-600">
              {place.place.name |> String.first() |> String.upcase()}
            </span>
            <div class="min-w-0 flex-1">
              <p class="truncate text-sm font-bold text-slate-900">{place.place.name}</p>
              <p class="mt-0.5 truncate text-xs text-slate-400">{place.place.slug}</p>
            </div>
            <span class={[
              "rounded-full px-2.5 py-1 text-[10px] font-bold ring-1 ring-inset",
              status_class(place.status)
            ]}>
              {status_label(place.status)}
            </span>
          </div>
        </div>
      </section>

      <section
        :if={capability?(@current_polo, :manage_billing)}
        id="payments-section"
        class="overflow-hidden rounded-xl border border-clubeira-line bg-white"
      >
        <div class="flex items-center justify-between border-b border-slate-100 px-5 py-4 sm:px-6">
          <div>
            <h2 class="text-sm font-bold text-slate-900">{gettext("Recent payments")}</h2>
            <p class="mt-1 text-xs text-slate-500">
              {gettext("Latest recorded financial events")}
            </p>
          </div>
          <span class="rounded-lg bg-orange-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-orange-700">
            {gettext("Finance")}
          </span>
        </div>
        <div id="payments-feed" phx-update="stream" class="divide-y divide-slate-100">
          <div
            id="payments-empty"
            class="hidden only:flex items-center gap-3 px-6 py-8 text-sm text-slate-500"
          >
            <.icon name="hero-banknotes" class="size-5 text-slate-300" />
            {gettext("No payments recorded in this polo.")}
          </div>
          <.link
            :for={{id, payment} <- @streams.payments}
            id={id}
            navigate={~p"/admin/payments/#{payment.id}?#{[polo: @current_polo.slug]}"}
            class="flex items-center gap-4 px-5 py-4 transition hover:bg-slate-50/80 sm:px-6"
          >
            <span class="grid size-10 shrink-0 place-items-center rounded-xl bg-orange-50 text-orange-600">
              <.icon name="hero-arrow-trending-up" class="size-5" />
            </span>
            <div class="min-w-0 flex-1">
              <p class="truncate text-sm font-bold text-slate-900">
                {money(payment.amount, payment.currency, @locale)}
              </p>
              <p class="mt-0.5 truncate text-xs text-slate-400">
                {payment.order.order_number} · {timestamp(payment.recorded_at, @locale)}
              </p>
            </div>
            <span class={[
              "rounded-full px-2.5 py-1 text-[10px] font-bold ring-1 ring-inset",
              status_class(payment.status)
            ]}>
              {status_label(payment.status)}
            </span>
          </.link>
        </div>
      </section>
    </div>

    <section
      :if={capability?(@current_polo, :manage_billing)}
      id="subscriptions-section"
      class="mt-6 overflow-hidden rounded-xl border border-clubeira-line bg-white"
    >
      <div class="flex items-center justify-between border-b border-slate-100 px-5 py-4 sm:px-6">
        <div>
          <h2 class="text-sm font-bold text-slate-900">{gettext("Recent subscriptions")}</h2>
          <p class="mt-1 text-xs text-slate-500">
            {gettext("Current contract and benefit-cycle snapshot")}
          </p>
        </div>
      </div>
      <div id="subscriptions-feed" phx-update="stream" class="divide-y divide-slate-100">
        <div
          id="subscriptions-empty"
          class="hidden only:flex items-center gap-3 px-6 py-8 text-sm text-slate-500"
        >
          <.icon name="hero-user-group" class="size-5 text-slate-300" />
          {gettext("No subscriptions recorded in this polo.")}
        </div>
        <div
          :for={{id, subscription} <- @streams.subscriptions}
          id={id}
          class="grid gap-3 px-5 py-4 transition hover:bg-slate-50/80 sm:grid-cols-[1.2fr_1fr_auto] sm:items-center sm:px-6"
        >
          <div class="min-w-0">
            <p class="truncate text-sm font-bold text-slate-900">{subscription.offering.name}</p>
            <p class="mt-0.5 truncate text-xs text-slate-400">
              {subscription.order.order_number}
            </p>
          </div>
          <div class="text-xs text-slate-500">
            <span class="font-bold text-slate-700">{subscription.balance.available_units}</span>
            {gettext("available units")}
          </div>
          <span class={[
            "w-fit rounded-full px-2.5 py-1 text-[10px] font-bold ring-1 ring-inset",
            status_class(subscription.status)
          ]}>
            {status_label(subscription.status)}
          </span>
        </div>
      </div>
    </section>
  </div>
</BackofficeComponents.shell>
